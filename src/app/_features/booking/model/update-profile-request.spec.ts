import { UpdateProfileRequest } from './update-profile-request';

describe('UpdateProfileRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateProfileRequest()).toBeTruthy();
  });
});
