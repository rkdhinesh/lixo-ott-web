import { Razorpayresponse } from './razorpayresponse';

describe('Razorpayresponse', () => {
  it('should create an instance', () => {
    expect(new Razorpayresponse()).toBeTruthy();
  });
});
